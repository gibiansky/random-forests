% load_keypoints    Returns a list of feature vectors and their classifications
% as positive or negative (1 or 0, respcetively).
function [vectors, classes] = load_keypoints()

[eyes, images] = load_data();

end
